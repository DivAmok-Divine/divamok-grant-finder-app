# Google AI Studio (Gemini) — get a free key at https://aistudio.google.com
# Used server-side only (the /api endpoint), never shipped to the browser.
GEMINI_API_KEY=your-key-here
GEMINI_MODEL=gemini-2.5-flash
